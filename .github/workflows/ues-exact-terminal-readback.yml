name: UES Exact Terminal Result Readback

on:
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      project:
        required: true
        type: choice
        options: [RP01, RP02, RP03, RP04]
      workstream:
        required: true
        type: string

permissions:
  contents: read

jobs:
  authorize:
    if: >-
      (github.event_name == 'issue_comment' && github.event.issue.number == 191 && github.actor == 'hamad933' && startsWith(github.event.comment.body, '/ues-exact-terminal-readback ')) ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    outputs:
      project: ${{ steps.command.outputs.project }}
      workstream: ${{ steps.command.outputs.workstream }}
    steps:
      - id: command
        env:
          EVENT_NAME: ${{ github.event_name }}
          COMMENT_BODY: ${{ github.event.comment.body || '' }}
          INPUT_PROJECT: ${{ inputs.project || '' }}
          INPUT_WORKSTREAM: ${{ inputs.workstream || '' }}
        run: |
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import os, re
          pattern = re.compile(r"/ues-exact-terminal-readback (RP01|RP02|RP03|RP04) ([A-Za-z0-9][A-Za-z0-9._:-]{0,127})")
          if os.environ["EVENT_NAME"] == "issue_comment":
              match = pattern.fullmatch(os.environ["COMMENT_BODY"].strip())
              if not match:
                  raise SystemExit("exact owner command required")
              project, workstream = match.groups()
          else:
              project = os.environ["INPUT_PROJECT"].strip().upper()
              workstream = os.environ["INPUT_WORKSTREAM"].strip()
              if project not in {"RP01","RP02","RP03","RP04"} or not re.fullmatch(r"[A-Za-z0-9][A-Za-z0-9._:-]{0,127}", workstream):
                  raise SystemExit("invalid exact project/workstream")
          print(f"project={project}")
          print(f"workstream={workstream}")
          PY

  readback:
    needs: authorize
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    concurrency:
      group: ues-terminal-result-exact-${{ needs.authorize.outputs.project }}-${{ needs.authorize.outputs.workstream }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e .
      - name: Consume one exact durable terminal lineage GET-only
        env:
          GITHUB_TOKEN: ${{ github.token }}
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
          UES_ALLOW_PUBLIC_SAME_REPO_STATE: "true"
          UES_STATE_REF_PREFIX: ues-runtime/v2
          PROJECT: ${{ needs.authorize.outputs.project }}
          WORKSTREAM: ${{ needs.authorize.outputs.workstream }}
        run: |
          set +e
          python -m ues.exact_terminal_readback "$PROJECT" "$WORKSTREAM" > "$RUNNER_TEMP/exact-terminal.json"
          status=$?
          set -e
          cat "$RUNNER_TEMP/exact-terminal.json"
          exit "$status"
      - name: Preserve sanitized exact result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ues-exact-terminal-${{ needs.authorize.outputs.project }}-${{ github.run_id }}
          path: ${{ runner.temp }}/exact-terminal.json
          retention-days: 7
          if-no-files-found: error
