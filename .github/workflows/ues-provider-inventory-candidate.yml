name: UES Provider Inventory Candidate

on:
  push:
    branches:
      - work/ues-v2-live-provider-ingestion-r01
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ues-provider-inventory-candidate-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prove:
    if: github.event_name != 'pull_request' || github.head_ref == 'work/ues-v2-live-provider-ingestion-r01'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install UES
        run: python -m pip install -e .

      - name: Full repository tests
        run: python -m unittest discover -s tests -v

      - name: Check Jules secret availability without disclosure
        id: jules_secret
        env:
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
        run: |
          if [ -n "$JULES_API_KEY" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            exit 1
          fi

      - name: Live Jules provider inventory read-only
        env:
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
        run: python -m ues.provider_inventory

      - name: Live waiting-session Activities reconciliation read-only
        env:
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
        run: python -m ues.provider_waiting
